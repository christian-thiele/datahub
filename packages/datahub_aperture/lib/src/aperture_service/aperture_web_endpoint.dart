import 'package:datahub/api.dart';

class ApertureWebEndpoint extends ApiEndpoint {
  ApertureWebEndpoint() : super(RoutePattern.any);

  @override
  Future<dynamic> get(ApiRequest request) async {
    
  }
}