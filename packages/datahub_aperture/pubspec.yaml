name: datahub_aperture
description: An easy to use administration frontend for DataHub services.
version: 0.1.0-dev.9
resolution: workspace

environment:
  sdk: ^3.9.0

dependencies:
  boost: ^2.0.0
  datahub: ^0.18.0-dev.18
  pointycastle: 4.0.0

dev_dependencies:
  build_runner: ^2.4.11
  datahub_codegen: ^0.18.0-dev.7
  datahub_postgres: ^0.18.0-dev.13
  lints: ^6.0.0
  test: ^1.26.2
