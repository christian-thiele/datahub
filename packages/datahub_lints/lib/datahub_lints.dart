/// The DataHub analyzer plugin.
///
/// Nothing here is imported by user code: the Dart Analysis Server loads this
/// package through `lib/main.dart`, whose path and `plugin` variable the
/// server generates code against. This library re-exports that entry point so
/// the package has a library named after itself.
library;

export 'main.dart';
